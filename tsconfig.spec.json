{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node", "jest", "express", "multer"],
    "strict": false
  },
  "include": [
    "src/**/*.spec.ts",
    "src/**/*.test.ts",
    "test/**/*.ts"
  ],
  "exclude": ["node_modules", "dist"]
}
